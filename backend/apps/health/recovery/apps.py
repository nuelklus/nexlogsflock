from django.apps import AppConfig


class AppsHealthRecoveryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.health.recovery"
    label = "health_recovery"

