from django.apps import AppConfig


class AppsFeedConsumptionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.feed.consumption"
    label = "feed_consumption"

